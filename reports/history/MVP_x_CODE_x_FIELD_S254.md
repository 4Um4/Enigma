# MVP x CODE x FIELD — матрица Фазы 1 (S254-сессия, 2026-09-11)
# Порядок Мастера: чтение mvp_secret_tavern ТЗ -> матрица -> repair -> гейт.
# Без правок кода. Без git. Полная версия — в сообщении сессии; здесь канон.

## Стратегический вывод
Три рельса: I) BODY->translator->DecisionHub-utility — МЁРТВ на последней
миле (F8+F9, S254-поле); II) needs->desires->activity->commitment — арбитраж
ПОЛЕ-GREEN (S203.2), субстрат dormant (S252, флаги OFF); III) эпистемический
(речь/память/trust/fear/последствия) — ПОЛЕ-GREEN (GC-11, S220, S253, L4-ноги).
MVP-секреты строится на III — НЕ блокирован F8/F9. EAT идёт по II — тоже
не блокирован; доказывает needs-петлю, не тело-as-constraint.

## Панель A: MVP P1-P11 — НЕ ПОСТРОЕН (все 11), с dead-wire-хвостами:
suppressed_secrets 0 читателей; player_beliefs computed-not-passed;
initial_holders 0 потребителей; P5-давление дормант (механизм+тесты без
production-wiring); ExitTrigger мёртв; matcher-лотерея жива.

## Панель B: живые рельсы MVP: trust-write (GC-11/ADR-O-367 поле),
reaction-дельты, речь-труба (S253+L4), память/L1, последствия, стили.

## Панель C: тело: оси/writer/translator GREEN; translator->hub RED (F8+F9);
player-visible OPEN; персистентность тела отсутствует (F1); сон частично
(D-MOM); needs-legacy жив (поле не сэмплировано); desires/EAT dormant,
субстрат полный; commitment-арбитраж GREEN.

## Класс «мёртвый провод»: 9 инстансов на одном аудите (F8, F9, GC09B-оракул,
suppressed_secrets, player_beliefs, initial_holders, P5-давление, ExitTrigger,
D-MOM). CAUSAL FALSE GREEN — доменный риск №1; S254-метод (A/A-зонд) —
обязателен в будущих гейтах.

## Repair-кандидаты: R1 де-грин оракула (тест-слой, ноль рантайма, санкция?);
R2 F9-нормализация+F8-consumer+оракул (мини-ADR, рантайм); R3 для MVP не нужен.
## Гейт: (1) MVP M1 (рекомендовано) / (2) R2 параллельно / (3) EAT
(с A/A-ревизией eat_vertical_test; fatigue->rest записи в каталоге нет).
