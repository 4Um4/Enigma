# ENIGMA Session State — 2026-06-04 12:11

Кампания: `Open_road` | Игрок: `Венус`

## ИДЕНТИФИКАЦИЯ АРХИТЕКТОРА

Прочитай эту секцию первой. Определи кто ты по задаче сессии:

- Если ты работаешь с Python-кодом, патчами, архитектурой, багами → **Архитектор #1 или #3**
- Если ты работаешь с UI, pygame, рендерингом, визуальными элементами → **Архитектор #2**
- Если ты работаешь с NPC-поведением, тиками, давлением, решениями → **Архитектор #3**

Прочитай свою секцию (#1, #2 или #3). У других архитекторов читай только строку "Сейчас делает:" — чтобы не конфликтовать по файлам.

---

## DNA — МЕТРИКИ ЗДОРОВЬЯ СИСТЕМЫ

_Сессия: 2.3 мин | Тиков: 4 | LLM-вызовов: 0_

| Метрика | Значение | Δ от прошлой | Интерпретация для LLM |
|---------|----------|--------------|----------------------|
| **SHI** (Simulation Health) | 100% | → +0.0% | ✅ норма: NPC активно принимают решения |
| **NPI** (NPC Pipeline) | 43% | ↑ +9.6% | ⚠️ 3/7 NPC с координатами: spatial pipeline частично сломан |
| **OBI** (Obedience) | 0% | → +0.0% | нет директив в сессии — OBI не применим |
| **SCF** (Spatial Coherence) | 0.0 | → +0.0 | ⛔ РАЗРУШЕНО: узлы не найдены → NPC не могут двигаться |
| **ADR** (Debt Ratio) | 0.00 | → +0.0 | нет ADR-записей — невозможно оценить |
| **CVS** (Causal Velocity) | 0.00/мин | → +0.0 | LLM не вызывалась: сессия без действий игрока |

_История: `reports/dna_history.jsonl` — 328 записей_

## #1 — АРХИТЕКТОР КОДА (патчи, файлы, архитектура)

### Сейчас делает:
3. **Каузальная целостность:** Формат отражает саму суть ENIGMA — система разделена на домены давления, а не на хронологию коммитов.

### Активные баги требующие патча:
- **[КРИТИЧНО]** `location_templates.json` недоступен — используется builtin fallback
  Проверка: `python -c "from app.services.scene.scene_state_manager import *; print('ok')"`
- **[КРИТИЧНО]** NPC `maid_lusya`: узел `bar_area` не найден в графе
  PowerShell: `Select-String -Path "backend/app/services/spatial/*.py" -Pattern "bar_area"`
- **[КРИТИЧНО]** NPC `thief_shadow`: узел `main_hall` не найден в графе
  PowerShell: `Select-String -Path "backend/app/services/spatial/*.py" -Pattern "main_hall"`
- **[КРИТИЧНО]** NPC `tavern_keeper_tornin`: узел `behind_bar` не найден в графе
  PowerShell: `Select-String -Path "backend/app/services/spatial/*.py" -Pattern "behind_bar"`
- **[КРИТИЧНО]** NPC `player`: узел `bar_area` не найден в графе
  PowerShell: `Select-String -Path "backend/app/services/spatial/*.py" -Pattern "bar_area"`


### Последние изменения (git log -5):
  - 95e3c20 docs: add branch DNA metric timeline
  - e898e8d snapshot: publish V.0.5.3.1.5 The Fool v2
  - 83e785c snapshot: update campaign_state.json
  - 6052a7f chore: ensure usurper.bundle is managed by git-lfs
  - 640541a chore: track usurper.bundle with git-lfs

### Последние записи MUTATIONS.md:
  - 1. **Контекстная локальность:** Чтобы починить баг с движением, ИИ читает секцию 1 и видит *всю* историю и *все* запреты без шума от починки UI.
  - 2. **Защита от легаси:** Секция явно показывает, что `LifeEngine` больше не бог (S32), а `SpatialQueryService` — авторитет (S37). ИИ не предложит вернуть `DIRECT_REFLEX`.
  - 3. **Каузальная целостность:** Формат отражает саму суть ENIGMA — система разделена на домены давления, а не на хронологию коммитов.

### Файлы с активными TODO/FIXME:
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\core\calendar.py
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\domain\behavior.py
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\domain\constants.py
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\domain\decision_context.py
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\domain\embodied_trace.py
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\domain\intent_profile.py
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\domain\movement.py
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\models\affect.py
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\models\cfrm.py
  - C:\DDD\Codex\VSC_Enigma\Enigma\backend\app\models\delta_payloads.py

---

## #2 — АРХИТЕКТОР UI (pygame, рендеринг, визуал)

### Сейчас делает:
3. **Каузальная целостность:** Формат отражает саму суть ENIGMA — система разделена на домены давления, а не на хронологию коммитов.

### Состояние рендеринга (из последней сессии игры):
- NPC с известными координатами (3):
  - `tavern_keeper_tornin`: x=17.6 y=2.1
  - `guard_borko`: x=19.5 y=4.2
  - `merchant_goran`: x=16.0 y=3.3
- NPC без координат (lerp не работает, 4):
  - `maid_lusya` (intent=FLEE)
  - `thief_shadow` (intent=OBSERVE)
  - `blacksmith_orm` (intent=OBSERVE)
  - `player` (intent=?)
- Граф-fallback локаций: нет

### Визуальные аномалии:
- spatial_fallback triggered: ⚠️ ДА
- Узлы не найдены (NPC не могут добраться до цели): 26

### Что НЕ трогать (сейчас меняет другой архитектор):
_(см. секции #1 и #3 — файлы backend/app/services/)_

---

## #3 — АРХИТЕКТОР СИМУЛЯЦИИ (NPC, тики, давление, решения)

### Сейчас делает:
3. **Каузальная целостность:** Формат отражает саму суть ENIGMA — система разделена на домены давления, а не на хронологию коммитов.

### Состояние симуляции (последняя сессия игры):

**Tick Pipeline:**
Тиков: 4 | Decisions > 0: 4/4 | LLM: 0 вызовов / 0 ответов | Симуляция: ✅ живёт
- LLM "Ничего не произошло": 0 раз
- LLM CJK-галлюцинации: 0 строк
- Стартап backend: ✅
- LLM сервер: ✅

**Предупреждения:**
  - ⚠️ КРИТИЧНО: 2 пред-шинных отказов (pipeline=0, causality=0, phase8=0, tick_orch=2) — CDS слеп к этим багам без Инварианта 3

**Movement Pipeline (по NPC):**
| NPC | Intent | Score | Traversal | Координаты | Виден игроку |
|-----|--------|-------|-----------|------------|--------------|
| blacksmith_orm | OBSERVE | 0.842 | ❌ | None | ❌ |
| guard_borko | OBSERVE | 0.978 | ❌ | x=19.5 y=4.2 | ❌ |
| maid_lusya | FLEE | 1.146 | ✅ | None | ❌ |
| merchant_goran | OBSERVE | 0.918 | ❌ | x=16.0 y=3.3 | ❌ |
| player | ? | 0.000 | ❌ узел не найден | None | ❌ |
| tavern_keeper_tornin | OBSERVE | 0.913 | ❌ узел не найден | x=17.6 y=2.1 | ❌ |
| thief_shadow | OBSERVE | 1.166 | ❌ узел не найден | None | ❌ |

**NPC с разрывом в pipeline (intent есть, traversal нет):**
  - `thief_shadow`: intent=OBSERVE, traversal=❌, coords=None
  - `blacksmith_orm`: intent=OBSERVE, traversal=❌, coords=None

### Каузальные разрывы:

#### [BREAK-2] Spatial Fallback
**Симптом:** `location_templates.json` недоступен → граф берётся из builtin
**Цепь отказа:**
  load_editor_json → fallback → builtin граф → узлы могут не совпадать с данными NPC
**PowerShell:** `Select-String -Path "backend/app/services/spatial/*.py" -Pattern "fallback"`

#### [BREAK-N] Узел `bar_area` не найден для `maid_lusya`
**Симптом:** NPC получил intent=FLEE но узел назначения отсутствует в графе
**Цепь отказа:**
  DECISION_HUB → SceneChange создан → MovementEngine.get_node('bar_area') → None
  → traversal не создан → координаты None → lerp не работает
**PowerShell:** `Select-String -Path "backend/app/services/spatial/graph_compiler.py" -Pattern "bar_area"`

#### [BREAK-N] Узел `main_hall` не найден для `thief_shadow`
**Симптом:** NPC получил intent=OBSERVE но узел назначения отсутствует в графе
**Цепь отказа:**
  DECISION_HUB → SceneChange создан → MovementEngine.get_node('main_hall') → None
  → traversal не создан → координаты None → lerp не работает
**PowerShell:** `Select-String -Path "backend/app/services/spatial/graph_compiler.py" -Pattern "main_hall"`

#### [BREAK-N] Узел `behind_bar` не найден для `tavern_keeper_tornin`
**Симптом:** NPC получил intent=OBSERVE но узел назначения отсутствует в графе
**Цепь отказа:**
  DECISION_HUB → SceneChange создан → MovementEngine.get_node('behind_bar') → None
  → traversal не создан → координаты None → lerp не работает
**PowerShell:** `Select-String -Path "backend/app/services/spatial/graph_compiler.py" -Pattern "behind_bar"`

#### [BREAK-N] Узел `bar_area` не найден для `player`
**Симптом:** NPC получил intent=? но узел назначения отсутствует в графе
**Цепь отказа:**
  DECISION_HUB → SceneChange создан → MovementEngine.get_node('bar_area') → None
  → traversal не создан → координаты None → lerp не работает
**PowerShell:** `Select-String -Path "backend/app/services/spatial/graph_compiler.py" -Pattern "bar_area"`

### Архитектурный долг (не трогать без обсуждения):
- Stale Cognition: DecisionHub работает на state T-1. Требует ADR-059.
- Cognitive Overlay Layer: отдельный спринт.

### Что НЕ трогать (сейчас меняет другой архитектор):
_(см. секции #1 и #2 — файлы frontend/)_