# backend/app/domain/__init__.py
# Назначение: Маркер пакета domain. Чистые доменные объекты, не зависящие от infrastructure.
# Зависимости: нет.
# ЗАЧЕМ: отделяем "что есть истина" от "как она течёт".
# domain не зависит от app.services, app.models, infrastructure.
# Любой сервис может импортировать отсюда, но не наоборот.