## Сводка для нового окна

### Что закрыто

**Фаза 10 Persistence (Фронт 1) — DONE**
- `PersistencePort.atomic_commit(scene_state, npc_states, events)` — контракт ABC, обе реализации (SQLite, JSON)
- `_phase_10_persistence()` в TickOrchestrator — вызывает `scene_manager.commit()` → `atomic_commit()`
- `_TickContext` — добавлены `npc_states`, `tick_events`
- Нарушение §4.2.1 в `routes.py:329` — удалён прямой `save_scene_state`, позиция пробрасывается через `ChatTurnRequest.player_position` → `_run_pipeline` → `init_scene_state` (в памяти, атомарный commit в конце)
- `hasattr`-хак и `save_all()` в SQLite-адаптере — удалены

### Два параллельных пути коммита (не resolved, просто зафиксировано)

| Путь | Endpoint | Коммит |
|---|---|---|
| Новый | `/game/idle_tick` → `TickOrchestrator.execute()` | `_phase_10_persistence` → `atomic_commit` |
| Старый | `/game/chat` → `GameLoop.run_turn()` → `_run_pipeline()` | `commit_tick()` → `atomic_commit` |

Оба теперь идут через контракт. Старый pipeline (`_run_pipeline`, `commit_tick`, `TickBuffer`, `phase_2_world_tick`) — legacy, живёт в `backend/app/services/game_loop/`. Новый (`TickOrchestrator`) — в `backend/app/core/tick_orchestrator.py`. Миграция run_turn на TickOrchestrator — отдельная задача.

### Что НЕ трогано (оставлено намеренно)

- `stream_turn` в `routes_stream.py` — не получает позицию, другой endpoint
- `save_scene_state` внутри `scene_init.py` — инициализация новых сцен
- `save_scene_state` внутри `scene_state_manager.py` — внутренние методы
- `JsonPersistenceAdapter.save_npcs` → `major_npcs.json` — легаси, смешивает static/runtime

---

### Что дальше — по диаграмме "Будущее v2"

Все 8 проверок — зелёные. Фронт 1 закрыт полностью.

---

## Приоритеты для нового окна

### P0 — Когнитивный цикл NPC (без них TickOrchestrator — скелет)

Фазы 3, 4, 6 сейчас `pass`. DecisionHub (фаза 5) работает, но на устаревшем state и без темы. Это прямые нарушения Устава.

**P0.1 — NPC states через контекст тика**
- Почему первым: prerequisite для фаз 3-6. Сейчас `ctx.npc_states = None` всегда. MemoryProcessor нечего апдейтить, TopicExtractor нечего читать, Phase 10 нечего сохранять.
- Что делать: LifeEngine возвращает `changes` (SceneChange), не full states. Нужна точка где собирается полный dict NPC states для контекста.
- Файлы: `tick_orchestrator.py` (_TickContext), `life_engine.py`, `npc_tick_pipeline.py`

**P0.2 — Фаза 3 Memory Processor**
- Почему: Устав §3.1 «DecisionHub работает на фазе 5, НЕ на фазе 3. Он читает СВЕЖИЙ state после MemoryProcessor. Лаг в 1 тик = баг.»
- Что делать: `MemoryProcessor.apply(event, npc_state)` — обновить NPCState ДО решения. Память уже завершена (из предыдущего фронта), нужно только повесить вызов.
- Файлы: `tick_orchestrator.py:220`, `services/memory/`

**P0.3 — Фаза 4 TopicExtractor**
- Почему: Устав §3.2 «CommunicationIntent.topic не может быть пустым.» Сейчас пустой → LLM галлюцинирует.
- Что делать: TopicExtractor читает STM + L2 из MemoryManager, формирует topic для каждого NPC.
- Файлы: `tick_orchestrator.py:227`, нужен `services/npc/topic_extractor.py`

**P0.4 — Фаза 6 IntentEventAdapter**
- Почему: Устав §3.3 «IntentEventAdapter — единственная точка превращения решения в событие.» Сейчас `pass` → решения NPC = сироты.
- Что делать: `CommunicationIntent → EventDTO` через IntentEventAdapter.
- Файлы: `tick_orchestrator.py:241`, нужен `services/events/intent_event_adapter.py`

---

### P1 — Чистка архитектуры (после P0)

**P1.1 — Миграция `run_turn` на TickOrchestrator**
- Удалить двойной pipeline: `_run_pipeline`, `commit_tick`, `TickBuffer`, `phase_2_world_tick.py`
- Один путь: `/game/chat` → `TickOrchestrator.execute()` → `_phase_10_persistence`
- Файлы: `game_loop/__init__.py`, `routes.py`

**P1.2 — `stream_turn` player_position**
- Пробросить `player_position` через `stream_turn()` → `_run_pipeline()` (пока жив старый pipeline) или напрямую в TickOrchestrator (после P1.1)
- Файлы: `routes_stream.py`, `game_loop/__init__.py`

---

### P2 — Технический долг

**P2.1** — `JsonPersistenceAdapter.save_npcs` → `major_npcs.json` — смешивает static/runtime
**P2.2** — `save_scene_state` внутри `scene_init.py` (строки 88, 184, 210) — перевести на `atomic_commit` для новых сцен
**P2.3** — `save_scene_state` внутри `scene_state_manager.py` (строки 331, 980, 1151, 1225, 1339, 1365) — внутренние методы, проверить что все callers идут через `commit()`

---

### Рекомендация

Открывать новое окно с **P0.1 — NPC states через контекст тика**. Это prerequisite для всего когнитивного цикла. Без него фазы 3, 4, 6 невозможно реализовать — нечего передавать в MemoryProcessor и нечего сохранять в Phase 10.

