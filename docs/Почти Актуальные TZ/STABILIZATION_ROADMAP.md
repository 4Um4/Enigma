# STABILIZATION ROADMAP (Post-Audit v0.5.3.7.10)

**Статус:** Фаза стабилизации и рефакторинга завершена. 
Фундамент жив, архитектура пайплайна, физика и эпистемическая труба работают. 
Инфраструктура LLM защищена от падений. God-методы разбираются. Проект собирается на чистой машине. IPT лог кристально чист (39/0).

---

## ✅ ЧАСТЬ 1: ЗАКРЫТЫЕ БАГИ И АРХИТЕКТУРНЫЕ ИСПРАВЛЕНИЯ

*Все критические и высокие баги из аудитов V0.5.3.7.2 - V0.5.3.7.10 закрыты.*
* **LLM & Cache:** C-09..C-12, H-06..H-08 (Cache hit-rate разблокирован, write/read в hot-path работает).
* **Replay & Determinism:** H-01..H-05, N-02, N-33 (Wall-clock eliminated в game_loop через `time.monotonic()`, injectable Clock работает).
* **Game-loop & Pipeline:** C-03..C-05, C-16, H-29..H-37 (Swallow patterns устранены, idle-tick стабилен, dead code удалён).
* **Memory & Beliefs:** C-15, H-41..H-43, N-28 (Мёртвые поля и методы очищены, `assess_beliefs` удалён).
* **Spatial & Movement:** N-30, N-31, Graph Compiler edge auto-generation fixed, map editor sync работает (кэш графа сбрасывается при сохранении).
* **KernelRNG:** H-26 (Salt добавлен во все 5 вызовов, коллизии потоков устранены, детерминизм восстановлен).
* **IPT Cleanup:** Лог инвариантов полностью очищен от ложных предупреждений (снижение уровня логирования, deduplication, удаление мёртвых ссылок).
*Ядро симуляции (Tick, Movement, Decision, LLM) стабильно. CI, зависимости, сериализация, инварианты — работают.*

---

## 🟡 ЧАСТЬ 2: ОСТАВШИЙСЯ ТЕХНИЧЕСКИЙ ДОЛГ (Не блокирует запуск)

* **Архитектурные (Sprint 1-4):** Декомпозиция god-файлов (`game_loop/__init__.py`, `life_engine.py`, `tick_orchestrator.py`). Унификация схем Belief/Memory (H-38..H-40, N-11, N-12). Введение `app/contracts/` Protocol.
* **P2-2 (mypy --strict 79 ошибок):** `mypy.ini` починен, но сам код содержит 79 ошибок типизации, особенно в `spatial_runtime.py` и `spatial_service.py`. Требует ручной правки типов.
* **P2-4 (print() в backend):** 76 вхождений `print()` в `backend/app` вместо `logger`.
* **P2-5 (TODO/FIXME):** 49 файлов с 64 маркерами в доменном слое. Требует разбора.

---

## 🚀 ЧАСТЬ 3: БУДУЩИЕ ЗАДАЧИ (Функционал для новых Эпох)

* **P1-24 (ConfessionParser):** Парсинг признаний NPC из LLM-ответов для извлечения секретов. Нужен для энд-скрина и механики расследования.
* **P1-25 (Semantic Cache):** Семантическое кэширование LLM-запросов (BGE embeddings + FAISS). Критично для производительности при масштабировании числа NPC.
* **P1-31 (ObservationLayer / BeliefProjector):** Унификация источников правды для убеждений NPC. Устраняет двойную запись в `BeliefState` (Фаза 3 vs Фаза 5). Задача для Эпохи 7.

---
**Вердикт:** База цела. Проект стабилен, архитектура чиста. Готов к наращиванию нового функционала.
```

Сохрани этот текст в `STABILIZATION_ROADMAP.md`, а старый аудит `TZ_ARCHITECTURE_AUDIT_V0.5.3.7.9.md` можешь смело перемещать в папку `docs/archive/`, так как он выполнил свою функцию и больше не отражает актуальное состояние кода.