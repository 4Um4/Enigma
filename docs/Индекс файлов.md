---

# ИНСТРУКЦИЯ ДЛЯ ПРОДОЛЖЕНИЯ

## ШАГ 1: ПЕРВАЯ КОМАНДА В НОВОМ ОКНЕ

```
Покажи текущее состояние проекта:
1. Сколько тестов проходит
2. Есть ли импорты на удалённые файлы
3. Есть ли TODO-заглушки в коде (не в README)
```

---

## ШАГ 2: ПЕРВЫЕ ПРОВЕРКИ

```powershell
python -m pytest backend/tests/ -q 2>&1 | Select-Object -Last 1
```

Ожидание: **230 passed, 10 skipped**

```powershell
Get-ChildItem "backend/app" -Filter "*.py" -Recurse | Select-String -Pattern "game_loop_factory|memory_manager_agent" | Select-Object Path
```

Ожидание: **пусто**

```powershell
Get-ChildItem "backend/app" -Filter "*.py" -Recurse | Select-String -Pattern "TODO.*временная заглушка|TODO.*будет удалено" | Select-Object Path, LineNumber, Line
```

Ожидание: **пусто или только легитимные** (active_traits — мост до ResonanceEngine)

---

## ЧТО БЫЛО СДЕЛАНО (контекст для LLM)

### Глобальная очистка
- Удалён `game_loop_factory.py` — мигрирован в `main.py` через `app.state`
- Созданы `game_loop_builder.py` + `game_loop_accessor.py`
- `routes.py` и `routes_stream.py` получают GameLoop через `Depends(get_game_loop)`
- Удалён дубликат `NPCStateL2` из `npc_profile.py` — теперь только в `npc_state.py`
- Удалён дубликат `NPCIdentityL1` из `npc_profile.py` — теперь только в `npc_state.py`
- `npc_profile.py` содержит ТОЛЬКО: `InventoryProfile`, `PsycheBase`, `NPCProfileL0`, `SpatialSnapshotR4`
- `npc_state.py` содержит ВСЁ остальное: `NPCState` (L2), `NPCIdentityL1` (L1), `NPCPersonality` (L0 алиас), `DecisionView`, `EventMemory`
- Удалены `react()` + `_fallback_react()` из `npc_agent.py` (-213 строк)
- Удалён мёртвый `to_legacy()` из `npc_state.py`
- `_request_legacy` переименован в `_request_sync` в `router.py`
- Удалены мёртвые параметры `extractor` + `life_engine` из `game_loop_builder.py`
- Исправлены ложные `[LEGACY]` теги: `context_builder.py` — активен

### StateApplicator Pipeline (КРИТИЧЕСКИЙ РАЗРЫВ ЗАКРЫТ)
- Добавлен `NPCState.write_to_legacy()` — записывает состояние обратно в legacy dict
- В `game_loop.py` после `StateApplicator.apply()` вызывается `write_to_legacy()`
- Изменённые NPC сохраняются через `_save_npcs()`
- **NPC теперь реально меняют стресс/интент после действий игрока**
- Интеграционный тест: `test_state_applicator_pipeline.py` (2 passed)

---

## АРХИТЕКТУРНЫЕ КОНТРАКТЫ (НЕ ЛОМАТЬ)

```
1. DecisionHub — ТОЛЬКО читает, НЕ пишет
2. StateApplicator — ТОЛЬКО пишет через apply(), НЕ решает
3. NPCState — иммутабелен, apply() возвращает копию
4. LLM — ТОЛЬКО вербализация, НЕ получает числа
5. GameLoop живёт в app.state, НЕ в глобальном синглтоне
6. npc_profile.py — ТОЛЬКО L0 типы (immutable)
7. npc_state.py — ВСЕ L1/L2 типы и адаптеры
```

---

## НА ЧЁМ ОСТАНОВИЛИСЬ

**Фаза 1 завершена.** Следующая — **Фаза 2: Commitment Model (R2.1)**

Цель: NPC имеют инерцию, не дребезжат между intent'ами.

```
Что добавить:
- commitment поле в Intent (инерция намерений)
- reactive_urgency + freshness_decay в DecisionHub
- Intent Saturation Penalty (защита от залипания)
```

---

## КЛЮЧЕВЫЕ ФАЙЛЫ

| Файл | Назначение |
|------|------------|
| `npc_state.py` | ВСЕ типы NPC (L0 алиас, L1, L2, Intent enum, WillState enum) |
| `npc_profile.py` | ТОЛЬКО L0 шаблон (NPCProfileL0, PsycheBase) |
| `decision_hub.py` | Формула score(), StateDeltas, DecisionResult, EventContext |
| `state_applicator.py` | apply() — ЕДИНСТВЕННАЯ точка записи |
| `game_loop.py` | Координатор, вызывает DM → DecisionHub → StateApplicator |
| `game_loop_builder.py` | Сборка GameLoop (вызывается из main.py startup) |
| `game_loop_accessor.py` | `get_game_loop(request)` через Depends |
| `main.py` | `app.state.game_loop = build_game_loop()` в startup |

---

## ПОРЯДОК РАЗВИТИЯ

```
ФАЗА 2: Commitment Model (R2.1) ← СЛЕДУЮЩАЯ
ФАЗА 3: Ego Resistance (R6), Verbalization (R3), Слухи (R7)
ФАЗА 4: World Director (R9) — фракции, фронты
```

---

## ПРАВИЛА РАБОТЫ

1. **НЕ проси файл целиком** — только нужные строки через PowerShell
2. **НЕ угадывай** — если не знаешь, спроси через команду
3. **ЗАМЕНИ вместо БЫЛО/СТАЛО** — пользователь копирует и заменяет
4. **ТИПИЗАЦИЯ обязательна** — аннотации входов/выходов
5. **КОММЕНТАРИИ на русском** — объясняют ЗАЧЕМ, не ЧТО
6. **ТЕСТ после изменения** — без тестов нет уверенности
7. **ОДИН шаг = ОДНО изменение** — не прыгать между модулями

---

## ПРИМЕР ПЕРВОГО ЗАПРОСА В НОВОМ ОКНЕ

```
Вот файлы: README.md, Игра.md, РЕЖИМ РАБОТЫ.md

Проверь состояние проекта по инструкциям из памяти.
Покажи: тесты, мёртвые импорты, TODO-заглушки.

После — продолжим Фазу 2 (Commitment Model R2.1).
```



✅ Фаза 2 (R2.1) завершена. Все тесты зелёные.

Итог Commitment Model
КомпонентФайлЧто сделаноintent_progress_ticksnpc_state.pyНовое поле + сериализация_apply_progress()state_applicator.pyСчётчик значимых дельт, мягкий сброс при смене intent_intent_inertia()decision_hub.pyРост → пик → decay только при effective_stall > 6
Поведение системы теперь:

NPC движется к цели → progress растёт → decay не включается → intent держится
NPC топчется на месте → effective_stall накапливается → после 6 тиков инерция убывает → intent меняется
Смена intent → progress × 0.3 (цепочки не ломаются)


Диагноз по логам:
Проблема
Причина в логах
Все отвечают	[GAME_LOOP] PerceptionFilter skip: recent=0 npc_ids=5 — фильтр пропускает ВСЕХ, каждый ход
Не знают пол	В промпте нет поля игрока/NPC (нужно проверить verbalization_context.py)
Не понимают отыгрыш	DM Router не различает "Я иду" vs "Помой"
Повторяются	intent=report, intent=trade каждый ход — DecisionHub не получает контекст смены
Забывают	L2 Event Memory не передаётся в промпт LLM
Борко говорит "Она Люся"	Нет адресации — NPC не знает, к кому обращён вопрос

Вердикт:
ДА, roadmap верная. Но следующий фокус — R3 Verbalization, а не R6 Ego Resistance.

Проблемы 1-6 решаются в R3:

Адресация (кто отвечает)
Контекст памяти в промпте
Пол и identidad в промпте
Различение отыгрыша/команды
Следующий шаг — посмотреть что LLM реально получает