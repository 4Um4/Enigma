# ТЕХНИЧЕСКИЙ ДОКУМЕНТ: Подсистема боя, Воля Аватара и Физиология (Версия 3.0 — Архитектурный Констракт)

**Статус:** К исполнению (Meta Code Architect Mode)
**Целевая аудитория:** LLM-ассистент, выполняющий интеграцию в кодовую базу ENIGMA V.0.5.2.4
**Главный принцип:** LLM = Voice, Python = Logic. Математика считает плоть, нейросеть озвучивает боль.

---

## 1. КРИТИЧЕСКИЕ АРХИТЕКТУРНЫЕ ИНВАРИАНТЫ (НЕНАРУШИМЫ)

Нарушение любого из нижеперечисленных пунктов = архитектурный баг и разрушение консистентности мира.

1.  **CombatResolver = Pure Function.** Внутри бойца нет состояния. На входе замороженные снимки состояния, на выходе — только дельта.
2.  **Три уровня защиты от мутации:**
    *   *Типы:* Только `frozen=True` dataclasses на входе и выходе (`NPCStateSnapshot`, `DamageDelta`). Физическая блокировка присваивания.
    *   *Изоляция RNG:* Случайность инкапсулирована. Запрещен глобальный `random()`. Используется инстанс `random.Random(rng_seed)`. Это дает детерминизм для тестов и защиту от побочных эффектов.
    *   *Возврат только Delta:* Внутри `CombatResolver` запрещены `db.write()`, `event_bus.publish()` и прямые изменения `NPCState`.
3.  **StateApplicator = Единственный мутатор.** Только `SqlitePersistenceAdapter.atomic_commit` применяет дельты к стейту (Устав §4.2.1).
4.  **EventBus = Транспорт.** Шина не трансформирует события. Вставлять логику классификации внутрь шины запрещено (Устав §5.1).

---

## 2. СИГНАТУРА БОЕВОГО ДВИЖКА (Строго к исполнению)

Реализовать функцию разрешения боя со следующей сигнатурой:

```python
# backend/app/services/combat/combat_resolver.py

from dataclasses import dataclass
from typing import Optional
from app.domain.body import InjuryDTO, BodyZone
from app.domain.deltas import DamageDelta
from app.domain.snapshots import NPCStateSnapshot  # НОВОЕ: frozen view
from app.domain.events import CombatIntentDTO       # НОВОЕ: DTO из шины

def resolve_combat(
    attacker:  NPCStateSnapshot,   # frozen — физически неизменяем
    defender:  NPCStateSnapshot,   # frozen — физически неизменяем
    intent:    CombatIntentDTO,    # frozen DTO из EventBus
    rng_seed:  Optional[int] = None, # None = непредсказуемо, int = детерминированный тест
) -> DamageDelta:                  # frozen — только описание изменений
    """
    (state_snapshot, intent) -> delta
    """
    ...
```

---

## 3. НОВЫЕ СУЩНОСТИ В `domain/` (Закон 1.2)

Все сущности физически неизменяемы (`frozen=True`).

```python
# domain/body.py
class BodyZone(Enum):
    HEAD = "head"
    THROAT = "throat"
    TORSO = "torso"
    GROIN = "groin"
    ARM_WIELD = "arm_wield"
    ARM_SHIELD = "arm_shield"
    LEGS = "legs"

@dataclass(frozen=True)
class InjuryDTO:
    zone: BodyZone
    severity: Literal["minor", "moderate", "severe", "critical", "fatal"]
    pain_value: float
    bleeding_rate: float      
    status_effect: Optional[str] # "stun", "knockout", "amputated"

# domain/deltas.py
@dataclass(frozen=True)
class DamageDelta:
    entity_id: str
    injuries: List[InjuryDTO]
    composure_delta: float
    blood_loss_delta: float
    pain_delta: float

# domain/avatar.py
@dataclass(frozen=True)
class AvatarNeedsDTO:
    hunger: float      
    fatigue: float     
    pain: float        
    sanity: float      

# domain/snapshots.py (КРИТИЧЕСКИ ВАЖНО)
@dataclass(frozen=True)
class NPCStateSnapshot:
    """Проекция NPCState, безопасная для передачи в Pure Functions."""
    entity_id: str
    hp: float
    pain: float
    composure: float
    # ... остальные поля, необходимые для расчета боя
```

---

## 4. ИНТЕГРАЦИЯ В ФАЗОВУЮ МОДЕЛЬ (Устав §3)

### Фаза 1: Input -> IntentPreParser (НОВОЕ)
Ввод игрока проходит через `core/intent_preparser.py` **ДО** попадания в шину.
1. RegEx словарь ищет маркеры насилия. Если найдено — жестко классифицирует как `COMBAT_ACTION`.
2. Fallback -> LLM intent classifier.
3. Результат: `CombatIntentDTO`.

### Фаза 1.5: WillpowerGate (НОВОЕ)
Интент игрока проходит через Врата Воли в `services/player_cognition/willpower_gate.py`.
*   Это фильтр: `EventDTO -> WillpowerGate -> EventDTO`.
*   **Comply:** Пропускает `PLAYER_ATTACK_CONFIRMED`.
*   **Refuse:** Блокирует атаку, генерирует `AVATAR_REJECTED_INTENT` (Аватар глумится, действие отменяется).

### Фаза 2 & 3: EventBus & Memory
Событие атаки летит в шину. `MemoryProcessor` обновляет стейт.

### Фаза 5: Decision
`DecisionHub` цели работает стандартно. Пьяный Борко не реагирует (интент `idle`).

### Фаза 6: Resolution (Бой)
Из текущего `NPCState` создаются `NPCStateSnapshot` (для атакующего и защищающегося).
Вызывается `resolve_combat(snapshot_attacker, snapshot_defender, intent, rng_seed)`.
Возвращается `DamageDelta`. 

### Фаза 8/9: StateApplicator
`StateApplicator` применяет `DamageDelta` к `NPCState` через `SqlitePersistenceAdapter.atomic_commit`.

---

## 5. ЗАЩИТА ОТ СКРЫТЫХ ПРОБЛЕМ (Крайне важно для реализации)

1.  **Pain feedback loop:** Боль не должна линейно снижать защиту (иначе цикл: боль -> меньше защиты -> еще урон -> еще боль).
    *Решение:* `pain_effect = log(pain + 1)`.
2.  **Bleeding = Таймер смерти:** Линейное кровотечение скучно.
    *Решение:* `bleeding_rate * clotting_factor(time)` (свёртываемость крови со временем снижает скорость потери крови, если рона не смертельна).
3.  **Willpower abuse:** Игрок спамит безумные действия.
    *Решение:* При каждом `refuse` Аватар получает `stress_d`. После 3 отказов — игнорирует игрока на N тиков (cooldown).
4.  **Corpse = Event bomb:** Труп вызывает панику у каждого входящего НПС.
    *Решение:* Труп излучает `WITNESS_DEATH` эвенты с `decay` (затухание). Через 3 тика паника сменяется привычкой.

---

## 6. ПЛАН ИНТЕГРАЦИИ (Строго пошагово, Режим работы п.2)

**ЗАПРЕЩЕНО перепрыгивать шаги.**

### Шаг 0: Инфраструктура Delta и Snapshots (Фундамент)
1. Создать `domain/deltas.py` с `DamageDelta`.
2. Создать `domain/snapshots.py` с `NPCStateSnapshot`.
3. Реализовать маппер `NPCState -> NPCStateSnapshot`.
4. Обеспечить поддержку `DamageDelta` в `SqlitePersistenceAdapter.atomic_commit`.

### Шаг 1: Intent PreParser (Вход)
1. Создать `core/intent_preparser.py`.
2. Добавить RegEx-словарь маркеров насилия.
3. Интегрировать перед `EventBus.publish` на Фазе 1.

### Шаг 2: Combat Skeleton (Базовый бой)
1. Создать `services/combat/combat_resolver.py` со строгой сигнатурой (см. раздел 2).
2. Логика: расчет Гауссова распределения попадания -> генерация `DamageDelta`.
3. Интегрировать вызов в Фазу 6 (через обработчик события `PLAYER_ATTACK_CONFIRMED`).

### Шаг 3: Физиология (Глубина)
1. Добавить `InjuryDTO`, `BodyZone` в `domain/`.
2. Расширить `CombatResolver` для расчета зон попадания и кровотечения.
3. Добавить `AvatarNeedsDTO` + тики голода/усталости в `LifeEngine` (Фаза 0).

### Шаг 4: Willpower Gate (Конфликт)
1. Создать `services/player_cognition/willpower_gate.py`.
2. Внедрить перехват `PLAYER_INTENT` на Фазе 1.5.
3. Реализовать исходы `Comply / Refuse / Counter-Offer`.

### Шаг 4: Смерть и Трупы (Последствия)
1. При `DEAD`: НПС меняет `EntityClass` на `CORPSE` (остается в `npc_positions`, излучает эвенты с decay).
2. Реализовать механику обыска трупа (Loot).

---
**Финальный вопрос к LLM-ассистенту перед стартом Шага 0:**
Подтверди готовность начать с создания `domain/deltas.py` и `domain/snapshots.py`. Укажи, как ты планируешь осуществлять маппинг `NPCState` в `NPCStateSnapshot` (отдельный метод в сервисе или встроенный класс-метод `to_snapshot` в моделях), чтобы гарантировать, что `CombatResolver` получит только неизменяемые данные.

---

# Устаревший документ по аватару:

Составляю архитектурный документ. Это не просто идеи, это строгий план интеграции в ENIGMA, который не нарушит Устав.

---

# ДОРОЖНАЯ КАРТА: Подсистема «Воля Аватара» и «Экономика Выживания»

**Версия:** 1.0
**Статус:** Архитектурный план
**Зависимости:** Устав ENIGMA §2, §3, §4

---

## 1. КОНЦЕПТУАЛЬНАЯ МОДЕЛЬ

Игрок (Source: `player`) и Аватар (NPC: `avatar_player`) — это два сознания в одном теле.
*   **Игрок** генерирует `IntentDTO` (желания).
*   **Аватар** — это физическая оболочка со своими потребностями (голод, сон, страх) и характером.
*   Если `IntentDTO` противоречит состоянию Аватара, `DecisionHub` Аватара отклоняет интент и генерирует собственный `CommunicationIntent` (отказ, глумление или торг).

**Правило:** Аватар — это такой же NPC, проходящий через 10 фаз тика, просто его `Input` (Фаза 1) формируется из текста игрока, а не из `WorldTick`.

---

## 2. НОВЫЕ СУЩНОСТИ В СЛОЕ DOMAIN (Закон 1.2)

Никакой логики, только данные. Добавляем в `backend/app/domain/`.

### 2.1 AvatarNeeds (Физиология)
```python
@dataclass(frozen=True)
class AvatarNeeds:
    hunger: float      # 0.0 (сыт) - 100.0 (голоден до смерти)
    fatigue: float     # 0.0 (бодр) - 100.0 (валится с ног)
    pain: float        # 0.0 (здоров) - 100.0 (агония)
    sanity: float      # 100.0 (вменяем) - 0.0 (безумец)
```

### 2.2 WillpowerCheckResult (Результат Воли)
```python
@dataclass(frozen=True)
class WillpowerCheckResult:
    success: bool
    resistance_level: Literal["comply", "reluctant", "refuse", "counter_offer"]
    avatar_reaction: str  # Тема для CommunicationIntent (например, "глумление над трусостью игрока")
    stress_delta: float   # Сколько стресса получил Аватар за подавление воли
```

---

## 3. ИНТЕГРАЦИЯ В ФАЗОВУЮ МОДЕЛЬ (Устав §3)

Аватар не ломает фазы, он использует их.

### ФАЗА 1: Input
Игрок пишет: *"Нападу на стражника"*.
Система создает стандартный `EventDTO(type=PLAYER_INTENT, payload={"action": "attack", "target": "guard_borko"})`.

### ФАЗА 3: Memory Phase
`MemoryProcessor` обновляет `NPCState` Аватара. Если Аватар голоден (`hunger > 70`), в его state появляется флаг `urgency: survive`.

### ФАЗА 4: Pre-Decision (Ключевая точка!)
`TopicExtractor` Аватара читает `PLAYER_INTENT` + свой `NPCState`.
*   Если Игрок хочет драться, а Аватар голоден и труслив, `TopicExtractor` формирует тему: **"отказ от боя из-за голода и страха"**.
*   Закон 2.2.1 соблюден: `topic` не пустой.

### ФАЗА 5: Decision (Механика Воли)
`DecisionHub` Аватара вызывает `WillpowerEngine.compute(intent, avatar_state)`.
1.  **Comply (Подчинение):** Воля игрока сильна. Аватар выполняет, но получает `stress_d`.
2.  **Reluctant (Нехотя):** Аватар выполняет, но `DecisionHub` генерирует `CommunicationIntent` с жалобой/стоном.
3.  **Refuse (Отказ):** Аватар игнорирует команду. Создается `CommunicationIntent` с глумлением ("Сам иди бейся, олимпиец, у меня руки трясутся!").
4.  **Counter-Offer (Торг):** Аватар предлагает сделку. ("Я ударю его, если ты заставишь Торнина накормить меня").

### ФАЗА 6: Post-Decision
`IntentEventAdapter` превращает решение Аватара в `EventDTO`.
*   Если `Comply` -> `EventDTO(type=PLAYER_ATTACKED)`.
*   Если `Refuse` -> `EventDTO(type=AVATAR_REJECTED_INTENT)`.

### ФАЗА 7: EventBus
События летят в шину. Если `AVATAR_REJECTED_INTENT`, фронтенд получает текстовый ответ от Аватара, а боевая система **не активируется**.

---

## 4. МЕХАНИКА ТОРГА (Обмен)

Это не отдельный режим, это цикл `EventDTO`.

1.  **Игрок:** "Укради у Горана кошелек" (`PLAYER_INTENT`).
2.  **Аватар (Фаза 5):** WillpowerCheck = `Counter-Offer`. Тема: "требую еды за риск".
3.  **Аватар (Фаза 7):** Публикует `EventDTO(type=AVATAR_COUNTER_OFFER, payload={"demand": "food", "action": "steal"})`.
4.  **Фронтенд:** Показывает текст Аватара: *"Сначала жратва, потом работа. Я не собака, чтобы служить за так"*.
5.  **Игрок:** "Ладно, я попрошу Торнина дать тебе похлебку" (`PLAYER_INTENT`).
6.  **Система:** Выполняет действие с Торниным. `NPCState` Аватара обновляется (`hunger` падает).
7.  **Игрок:** "Теперь кради" (`PLAYER_INTENT`).
8.  **Аватар (Фаза 5):** WillpowerCheck = `Comply` (голод утолен, риск оправдан).

---

## 5. ПРОВЕРКА НА АРХИТЕКТУРНЫЙ УСТАВ

| Закон Устава | Риск нарушения | Как избегается |
| :--- | :--- | :--- |
| **1.2 (domain не знает сервисов)** | Сущность Воли может залезть в логику | `WillpowerCheckResult` — чистый dataclass. Вычисления только в `services/` |
| **2.1.1 (Только EventDTO)** | Попытка напрямую передать отказ на фронт | Отказ Аватара — это `EventDTO(type=AVATAR_REJECTED)`, летит через шину |
| **2.2.1 (topic не пустой)** | Аватар молчит при отказе | `TopicExtractor` всегда генерирует тему отказа на Фазе 4 |
| **3.1 (Decision на свежем state)** | Проверка Воли до обновления голода | WillpowerEngine вызывается строго на Фазе 5, после Memory (Фаза 3) |
| **7.2 (Пустой topic)** | Система не понимает, почему Аватар отказал | Тема формируется строго из таблицы потребностей: `topic = f"отказ_из_за_{need}"` |

---

## 6. ПЛАН РЕАЛИЗАЦИИ (Шаги)

Работаем строго пошагово (Режим работы п.2).

### Шаг 1: Расширение NPCState (Domain/Models)
*   Добавить `AvatarNeeds` в `NPCState`.
*   Настроить `LifeEngine` (Фаза 0), чтобы каждый тик увеличивал `hunger` и `fatigue` у Аватара.

### Шаг 2: WillpowerEngine (Services)
*   Создать `backend/app/services/player_cognition/willpower_engine.py`.
*   Логика: `compute(intent, state) -> WillpowerCheckResult`.
*   Пока без торга, только 3 исхода: `comply`, `reluctant`, `refuse`.

### Шаг 3: Интеграция в DecisionHub (Фаза 5)
*   Если `source == player`, `DecisionHub` делегирует проверку `WillpowerEngine`.
*   Если `refuse`, хаб принудительно ставит `intent_type = "reject"`.

### Шаг 4: TopicExtractor для отказов (Фаза 4)
*   Добавить словарь: `{hunger: "желудок сводит от голода", fear: "поджилки трясутся"}`.
*   При формировании темы для отказа, ТопикЭкстрактор берет причину из словаря.

### Шаг 5: Обработка на Фронтенде (UI)
*   `api_client.py` обрабатывает `AVATAR_REJECTED_INTENT`.
*   Вывод текста Аватара особым цветом (например, фиолетовым, как голос в голове), чтобы игрок понимал, что это сказал не мир, а его собственное тело.

### Шаг 6: Механика Торга (Counter-Offer)
*   Добавить `Counter-Offer` в `WillpowerEngine`.
*   Игрок должен иметь возможность в тексте дать обещание, которое запишется в `MemoryManager` как факт, влияющий на проверку Воли на следующем ходу.

---

Жду твоей команды. Начинаем с Шага 1 (добавление `AvatarNeeds` в модели) или сначала хочешь обсудить логику формул для `WillpowerEngine`?