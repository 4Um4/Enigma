stream_turn(action_text="Люся тебя ударил Шэдоу?")
│
├─[324] classify_action(action_text) ──── UI BADGE (frontend)
│       └─ _router._classify_action(action_text)
│               └─ ВЫВОД: "player_attacks" → badge в UI
│
└─[407] _run_pipeline()
        │
        ├─[534] raw_input = actions[0].action  ← ТЕ ЖЕ ТЕКСТ
        │
        └─[644] dm_orchestrator.process_player_action(raw_input=raw_input, ...)
                │
                └─[75] _router.parse_and_validate(raw_input=raw_input, ...)
                        │
                        └─[151] event_type = _classify_action(raw_input)
                                │
                                └─ ВЫВОД: "player_attacks" → shared_context["action_type"]
                                        │
                                        └─[672] _raw_type = shared_context.get("action_type")
                                            └─ EventBus, SceneEvents, NPC цикл




[ВВОД ИГРОКА: "Её кто-то ударил? Это был ты Торнин?"]
 │
 ▼
1. backend/app/services/game_loop.py (Функция: stream_turn)
   │   (Принимает сырой текст. Понимает, что нужно узнать тип действия)
   │
   ├──► 2. backend/app/services/action/dm_orchestrator.py (Функция: classify_action)
   │       │   (Оркестратор получает текст и делегирует задачу роутеру)
   │       │
   │       └──► 3. backend/app/services/action/dm_router.py (Функция: _classify_action)
   │               (Именно здесь текст читается на наличие звездочек *...*. 
   │                Возвращает "player_interacts" обратно в Оркестратор, а тот — в Game Loop)
   │
   ▼   (Текст возвращается в game_loop.py, запускается тяжелый конвейер: _run_pipeline)
   │
   ├──► 4. backend/app/services/action/target_extractor.py (Класс: PlayerTargetExtractor)
   │       (Game Loop передает текст сюда. Скрипт ищет имена NPC. 
   │        Находит "Торнин" -> возвращает target_id="tavern_keeper_tornin")
   │
   ├──► 5. backend/app/services/action/dm_orchestrator.py (Функция: process_player_action)
   │       │   (Game Loop снова вызывает Оркестратор, чтобы собрать сцену)
   │       │
   │       └──► 6. backend/app/services/action/dm_scene_builder.py (Функция: enrich_raw_event)
   │               (Считает интенсивность действия, формирует контекст сцены. 
   │                Возвращает dm_result обратно в Game Loop)
   │
   ├──► 7. backend/app/services/events/event_bus.py (Функция: publish)
   │       (Game Loop берет текст и тип действия, и кричит на всю игру: 
   │        "Игрок сказал это Торнину!". Чтобы другие системы могли отреагировать)
   │
   ├──► 8. backend/app/services/npc/decision_hub.py (Функция: process_event)
   │       (Game Loop передает текст и данные в мозг Торнина. 
   │        Здесь считается математика: память L2, черты L0. Выдается намерение: DENY_ANGRILY)
   │
   └──► 9. backend/app/services/verbalization/verbalization_context.py
           (Game Loop передает текст игрока и намерение Торнина сюда. 
            Формируется системный промпт для нейросети)
            │
            └──► 10. LLM (llama_server / Qwen)
                     (Нейросеть читает промпт и генерирует финальный текст ответа)