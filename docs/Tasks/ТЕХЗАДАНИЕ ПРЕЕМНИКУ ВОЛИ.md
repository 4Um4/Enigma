Терминал преподал суровый урок. Мы нащупали даже не разрыв, а **онтологическую подделку**. 

Волю называют "Двигателем Сохранения Идентичности", но на деле **Воля слепа к прошлому**, а **Аватар бесчувствен к настоящему**. 

Вот три железобетонных диагноза:

1.  **Амнезия Сопротивления (WillpowerGate Amnesia):** `will.py` вернул **пустоту** по запросу памяти. WillpowerGate вычисляет `resistance` *только* на основе сиюминутного `psyche.identity_rigidity` и давления. Если Игрок предал NPC вчера, Воля NPC *не снизит* сопротивление приказам предателя сегодня. Аффект (`affect.py`) искажает давление (усиливает страх), но сама **жесткость идентичности не деградирует от травмы**.
2.  **Слепота Аватара (CFRM Player Blindness):** `local_causal_solver.py` подтвердил — Игрок является только `fear_of_player` (источником). Он **не является наблюдателем**. Аватар не получает `PsychologicalPressure` от паники толпы, криков или крови вокруг. Он иммунен к среде.
3.  **Незамкнутая Идентичность (Identity Delta Gap):** `decision_hub.py` сам признался в комментариях: `DeltaDomain.IDENTITY пока не генерируется`. Память влияет на Utility (через `_memory_penalty`), но не меняет саму структуру личности NPC в онтологии.

Это крах Философии Раздела 1. Воля не защищает Идентичность, потому что Идентичность — это статик из конфига, а не результат опыта.

Ниже — полностью переписанный файл `ТЕХЗАДАНИЕ ПРЕЕМНИКУ ВОЛИ.md`, где эти разрывы зафиксированы как критические архитектурные баги.

***

```markdown
# ТЕХЗАДАНИЕ ПРЕЕМНИКУ — Подсистема Воли (WillpowerGate & Hybrid Consciousness)

**Дата обновления:** 21.05.2026  
**Спринт:** 47 (Каузальное Замыкание Воли & Identity/Closure Audit)

---

## 1. Философия системы Воли

Система Воли в ENIGMA — это **не** моральный компас и **не** шанс блока действия. Это **Dynamic Identity Preservation Engine** (Двигатель Сохранения Идентичности). 

Аватар игрока — это Hybrid Consciousness Entity (ADR-031). Игрок генерирует намерения (внешняя воля), но применение интента проходит через психику аватара. Аватар спрашивает: *"Останусь ли Я СОБОЙ после этого действия?"*

**Ключевые принципы:**
1. **Травма ≠ страх.** Травма — это искажение интерпретации реальности (ADR-037). Страх — это эмоция. Воля работает с давлением и деформацией идентичности.
2. **Willpower — это инерция, а не порог.** Высокий willpower не блокирует действие, он замедляет ломку. Когда прорывает — катастрофически (состояние BROKEN/CONDITIONED).
3. **Страх — это интеграл угрозы по времени (ADR-049).** Эмоция не возникает от события. Она рождается из накопленного `affective_load`, когда давление превышает личностный порог.
4. **Моторное воплощение.** Конфликт воли — это не текст в логе. Это физическое сопротивление интерфейса ввода (`Resistance Medium`). Игрок должен стереть навязанный текст.
5. **Соматическое Вето.** Разум бессилен без тела. Травма, боль и шок — это физические ограничители (Feasibility Constraints). Сломанное тело не может исполнить приказ даже сломленной воли.
6. **Идентичность — это функция опыта.** Воля не может защищать то, что не меняется. Если предательство не снижает жесткость идентичности (resistance) перед предателем, воля — косметика.

---

## 2. Трубопровод Воли (The Will Pipeline)

Труба замкнута от сырого текста до пикселей. Разрыв любого звена = смерть симуляции.

### Шаг 1: Слой 1 — Semantic Compression (ADR-035)
**Файл:** `backend/app/services/input/intent_compressor.py`
- **Вход:** Сырой текст ("Я бью борко кружкой").
- **Fast Path:** `pymorphy3` лемматизация. Извлекает глаголы и существительные-цели.
- **Slow Path:** LLM через `LLMCompressorClient` (DI). Извлекает `target_reference` (строка!), `EmotionalVector`.
- **Выход:** `IntentSemanticField` (вероятностное поле, а не команда). Если LLM ошиблась — `UNCERTAIN`, а не безопасное OBSERVE.

### Шаг 2: Слой 2 — Target Resolution
**Файл:** `backend/app/services/game_loop/phase_1_input.py`
- **Вход:** `IntentSemanticField`.
- **Действие:** Fuzzy matching `target_reference` (строка) -> `target_id` (NPC ID).
- **Выход:** Заполненный `IntentParametersDTO` (строгий транспорт вместо `Dict[str, Any]`).

### Шаг 3: Семантический Транслятор — IntentPressureResolver (ADR-031)
**Файл:** `backend/app/services/will.py`
- **Вход:** `IntentParametersDTO` (semantic_action, target_id, physical_force).
- **Действие:** Переводит *тип действия* в *вектор давления на психику*.
- **Выход:** `IntentPressureProfile` (violence, humiliation, self_risk, moral_violation, identity_deviation).
- **Критический фикс:** Функция `resolve_intent_pressure` обязана распознавать ключи `player_attacks`, `player_threatens`. Использование старых ключей без алиасов запрещено.

### Шаг 4: Аффект — Resonance & Distortion (ADR-037)
**Файл:** `backend/app/services/affect.py`
- **Вход:** `IntentPressureProfile` + `AffectiveImprints` (память травм).
- **Слой 1 (`scan_affective_resonance`):** Ищет совпадение паттернов. Возвращает `ResonanceProfile`.
- **Слой 2 (`distort_pressure`):** Искажает базовое давление через `ResponseBias` (aggression→ярость, fear→подчинение).
- **Выход:** `AmplifiedPressureProfile`.
- **Разрыв:** Аффект искажает *давление*, но не меняет *жесткость идентичности* (WillpowerGate).

### Шаг 5: WillpowerGate — Cumulative Strain Model & Somatic Veto (ADR-031)
**Файл:** `backend/app/services/will.py`
- **Вход:** `AmplifiedPressureProfile` + `NPCState.psyche` + `NPCState.body_state` (Physiology Veto).
- **Формула:** `resistance = pressure.identity_deviation * psyche.identity_rigidity + ...`
- **Выход:** `WillResponseDTO` (state, resistance, identity_damage, generated_emotions, counter_offer).
- **Шкала деградации:** COMPLY → RELUCTANT → DISTRESSED → PANICKED → DISSOCIATING → BROKEN → CONDITIONED.
- **Counter-Offer:** Аватар всегда предлагает альтернативу (убежать, подкупить), пытаясь выжить.
- **Somatic Veto (Feasibility Constraints):** Прежде чем воля начнет сопротивление, физическое тело должно оценить выполнимость. `PhysiologyPayload` генерирует `ActionSpaceCompression`. При `pain > 0.7` — `APPROACH/ATTACK` feasibility = 0.0. При `shock > 0.8` — все намерения, кроме `IDLE/FALL`, feasibility = 0.0.
- **Разрыв:** Воля не получает L2 Память. Предательство не снижает `rigidity` перед предателем.

### Шаг 6: Фазовый Шлюз — Phase 1 Boundary Adapter (ADR-034)
**Файл:** `backend/app/services/game_loop/phase_1_input.py`
- **Действие:** `game_loop` вызывает чистую функцию `resolve_player_intent()`.
- **Запрет:** В `game_loop` НЕТ бизнес-логики воли, боли, страха. Только `resolve -> publish resolution`.
- **Выход:** `IntentResolution` DTO. Публикуется в EventBus (WILL_CONFLICT) и кладется в `shared_context`.

### Шаг 7: Каузальное Замыкание — TickOrchestrator
**Файл:** `backend/app/services/tick_orchestrator.py`
- `CombatSubscriber` реагирует на `PLAYER_ATTACKED`. Если `target_id` пуст, срабатывает fuzzy-matching по `target_reference`.
- `ImpactEngine` генерирует `PhysiologyPayload` (pain, shock_impulse).
- `ReactionSubscriber` читает `shock_impulse` из материализованных дельт и генерирует страх.
- **Ингибирование Воли Болью (Phase 8 Gating):** `DirectiveInterpretationSubscriber` ОБЯЗАН читать `ctx.physical_deltas_materialized`. При `shock_impulse > 0.5` подчинение воле (`obedience_intensity`) должно ингибироваться.
- **Единственная точка Causal Resolution:** Фаза 1 не вычисляет волю дважды (ADR-036).

### Шаг 8: Восприятие — PerceptionPayload & Acoustic Closure (ADR-042, ADR-049)
**Файл:** `backend/app/services/tick_orchestrator.py` (`_phase_9_integration`)
- **Порядок природы:** Реальность сначала обновляет восприятие, а не эмоции.
- CFRM вычисляет `PsychologicalPressure`, конвертирует его в `PerceptionPayload`.
- **Акустическое Замыкание (Acoustic Closure):** Речь NPC обязана публиковаться как `EventDTO` типа `NPC_SPOKE`, деобъектифицироваться в `FieldDisturbance(ACOUSTIC)` и попадать в `LocalCausalSolver`.
- **Слепота Аватара (CFRM Player Blindness):** Игрок НЕ является наблюдателем в `LocalCausalSolver`. Он не получает `PsychologicalPressure` от паники толпы или криков. Требуется интеграция `npc_id="player"` в цикл наблюдателей.
- **Мост Аффективного Аккумулятора (ADR-049):** `AffectiveIntegrator` накапливает `threat_gradient` во времени. При пересечении порога генерируется `EmotionPayload`.

### Шаг 9: Фронтенд — Embodied Perception (ADR-039)
**Файлы:** `frontend/presentation_firewall.py`, `frontend/perceptual_momentum.py`, `frontend/text_input.py`
- **Presentation Firewall:** Отсекает категориальные енумы. Пропускает только скаляры 0.0-1.0.
- **Perceptual Momentum:** Темпоральная инерция (S-curve). 
- **Resistance Medium:** `TextInput.infect()`. При WILL_CONFLICT поле ввода заполняется текстом аватара.
- **Статус:** ВЫКЛЮЧЕНО ИЗ РЕНДЕРА (`UNWIRED`).

---

## 3. Критические архитектурные законы (Запреты)

1. **Запрет Double Invocation (ADR-036):** WillpowerGate вызывается ОДИН раз за цикл.
2. **Запрет Domain Leakage:** `CombatSubscriber` НЕ пишет в Emotion. Только `PhysiologyPayload`.
3. **Запрет Retro-simulation (ADR-047):** `TICK_CATCHUP` УДАЛЁН.
4. **Запрет Spatial Divergence (ADR-048):** Чтение позиции из `scene_state["player_spatial"]` ЗАПРЕЩЕНО.
5. **Запрет LOD0/LOD1 Corruption (ADR-060):** Физики навигации и рулежки строго разделены.
6. **Закон Шейдера (ADR-039):** Фронтенд лишен права семантической интерпретации.

---

## 4. Известные архитектурные риски и баги

1. **~~Разрыв Perception→Emotion~~:** ЗАКРЫТ (ADR-049).
2. **~~LegacyStateDeltaAdapter~~:** ЗАКРЫТО.
3. **Фронтенд UNWIRED (Embodiment Gap):** ⚠️ АКТИВНЫЙ. Скаляры передаются в обход Firewall/Momentum. Инфекция ввода не происходит.
4. **Target Resolution Fragility:** ⚠️ АКТИВНЫЙ. Требуется контекстный резолв (Слой 2.5).
5. **~~LOD0/LOD1 Interrupt Logic~~:** ЗАКРЫТ (ADR-060.1).
6. **~~DirectiveInterpretationSubscriber State Desync~~:** ЗАКРЫТ (Спринт 47).
7. **~~Memory Pipeline Amnesia~~:** ⚠️ ЧАСТИЧНО ЗАКРЫТ. Память идет в `DecisionHub` (`_memory_penalty`), но **НЕ идет в WillpowerGate**. Воля слепа к истории.
8. **~~SCF=0 (Spatial Coherence Failure)~~:** ЗАКРЫТ (Спринт 44).
9. **~~SHI=0% (Глухая Воля)~~:** ЗАКРЫТ (Спринт 45).
10. **Синдром Глухого Общества (Verbalization Echo Gap):** ⚠️ АКТИВНЫЙ. Речь NPC — тупик, не порождает `ACOUSTIC` возмущений.
11. **Асимметрия Власти (Player-Only Social Physics):** ⚠️ АКТИВНЫЙ. NPC не могут отдавать приказы друг другу.
12. **Приказ сквозь Боль (Phase 8 Gating Gap):** ⚠️ АКТИВНЫЙ. Шок не ингибирует `directive_obedience`.
13. **Синдром Бессмертного Бегуна (Somatic-Decision Disconnect):** ⚠️ АКТИВНЫЙ. Тело не vetoирует действия в DecisionHub.
14. **Интонация Агонии (Polite Agony):** ⚠️ АКТИВНЫЙ. LLM не знает о травмах NPC.
15. **Амнезия Сопротивления (WillpowerGate Amnesia):** ⚠️ АКТИВНЫЙ. `WillpowerGate` не читает L2 Память. Предательство не снижает `rigidity`. Воля защищает статичный конфиг, а не живой опыт.
16. **Слепота Аватара (CFRM Player Blindness):** ⚠️ АКТИВНЫЙ. Игрок не является наблюдателем в `LocalCausalSolver`. Он иммунен к давлению толпы и панике.
17. **Незамкнутая Идентичность (Identity Delta Gap):** ⚠️ АКТИВНЫЙ. `DeltaDomain.IDENTITY` не генерируется. Опыт не меняет структуру личности онтологически.

---

## 5. Приоритеты следующего спринта

1. **[БАГ 3] Embodiment Wiring:** Подключить `PresentationFirewall`, `PerceptualMomentum` и `TextInput.infect` к `GameScreen`.
2. **[БАГ 10] Acoustic Closure:** Замкнуть петлю `Verbalization → EventDTO(NPC_SPOKE) → FieldDisturbance(ACOUSTIC)`.
3. **[БАГ 11] NPC-to-NPC Directives:** Вычислять легитимность на основе `relationship_cache` между `source_id` и `target_id`.
4. **[БАГ 12] Shock Inhibition:** Шок должен глушить `obedience_intensity` в `DirectiveInterpretationSubscriber`.
5. **[БАГ 13] Somatic Closure:** Создать мост `PhysiologyPayload` → `DecisionContext.compression.constraints`.
6. **[БАГ 14] Agony Verbalization:** Обогатить `VerbalizationContext` скалярами из `PhysiologyPayload`.
7. **[БАГ 15] Memory-Rigidity Coupling:** Внедрить чтение L2 Памяти (факты предательств, долгов) в `WillpowerGate`. Историческая травма должна снижать `identity_rigidity` перед источником травмы.
8. **[БАГ 16] Player as Observer:** Добавить `player` в список наблюдателей `LocalCausalSolver`. Аватар должен получать `PerceptionPayload` от массовых событий (паника, крики).
9. **Target Resolution Context (Слой 2.5):** Реализовать контекстный резолв цели.
10. **Spatial Player Resolution:** Починка резолва позиции игрока для NPC.

---

## 6. Критические файлы для старта

**Backend (Will Core):**
- `backend/app/services/will.py` (Транслятор, WillpowerGate, Amnesia fix)
- `backend/app/services/affect.py` (Аффект, Резонанс)
- `backend/app/models/will.py` (IntentPressureProfile, WillState, WillResponseDTO)

**Backend (Affective Accumulator - ADR-049):**
- `backend/app/services/affective/affective_integrator.py`
- `backend/app/services/affective/emotion_transition.py`

**Backend (Pipeline & Integration):**
- `backend/app/services/game_loop/phase_1_input.py`
- `backend/app/services/tick_orchestrator.py`
- `backend/app/services/combat/combat_subscriber.py`
- `backend/app/services/events/reaction_subscriber.py`
- `backend/app/services/social/directive_interpretation_subscriber.py`
- `backend/app/services/npc/state_applicator.py`
- `backend/app/services/npc/decision_hub.py` (Somatic Veto, Identity Delta)
- `backend/app/services/cfrm/local_causal_solver.py` (Player Observer fix)
- `backend/app/services/verbalization/state_interpreter.py`

**Frontend (Embodiment):**
- `frontend/presentation_firewall.py`
- `frontend/perceptual_momentum.py`
- `frontend/text_input.py`
- `frontend/game_screen.py`

**Domain & DTO:**
- `backend/app/domain/intent_profile.py`
- `backend/app/models/state_delta.py` (DeltaDomain.IDENTITY generation)
- `backend/app/models/delta_payloads.py`
- `backend/app/models/cfrm.py`
- `backend/app/models/npc_state.py`