# ТЕХЗАДАНИЕ ПРЕЕМНИКУ: Сессия S85 — Semantic Spatial Binding & Debt Cleanup

**Статус:** S83.1 дал Tick Coherence. Археология S85 вскрыла корневой разрыв навигации и накопленный техдолг фронтенда/географии. 
S85 устраняет **корневой разрыв навигации** (от L3 к Spatial Roles) и чистит критические легаси-фоллбэки.

**Главный принцип:** Архитектура ENIGMA работает (L3, Causal Kernel, Guards на месте). Проблема не в фундаменте, а в отсутствии дверей между комнатами и в гниющих мостах (фоллбэках).

---

## 0. АРХИТЕКТУРНЫЙ ВЕКТОР
*(Оставается прежним, см. предыдущую версию)*

---

## 1. ЧТО СДЕЛАНО (Кумулятивно)
*(Оставается прежним, см. предыдущую версию)*

---

## 2. АРХЕОЛОГИЧЕСКИЕ ОТКРЫТИЯ — АКТУАЛЬНО

### Открытие 1: SpatialService не реализует Role-Based Routing
*(Без изменений: `LifeEngine` вызывает несуществующий `resolve_node(role=...)`, фоллбэчит в хардкод `bar_area`)*

### Открытие 2: GraphCompiler УЖЕ создает правильный alias_map
*(Без изменений)*

### Открытие 3: _resolve_position слеп к графу
*(Без изменений: фоллбэк в `common_area`)*

### Открытие 4: Bug Risk 999.0 в Player Target Pipeline
В `player_target_pipeline.py` (строка 177) обнаружен маркер: `continue # Нет дистанции — не включаем (bug_risk: 999.0 ломает фильтр видимости)`. Если дистанция не вычислена, подставляется 999.0, что ломает логику видимости.

### Открытие 5: Нарушение границы Backend-Frontend (JSON Mirror)
`scene_state_manager.py` (строка 415): `# TODO-A1: JSON mirror — game_screen ещё читает файлы, не API. Удалить после A1.`
Бэкенд до сих пор выступает в роли файлового сервера для фронтенда, нарушая Закон 6.1 (Frontend работает только с DTO через API).

### Открытие 6: Легаси-рабство GameScreen
`game_screen.py` содержит каскад фоллбэков для устаревших структур:
- Строка 954: `Legacy fallback: list → конвертируем в dict`
- Строка 975: `Fallback: deprecated top-level npc_positions`
Это делает фронтенд хрупким и зависимым от формата dict, а не от строгих DTO.

### Открытие 7: Повсеместные заглушки в Sprite Resolver
`sprite_resolver.py` использует один и тот же спрайт (`Deadbeat/deadbeat_b`) для дверей, лестниц, бара, украшений и сердец (строки 33-63). Это делает невозможным визуальное различение объектов в игре.

---

## 3. ПОЛНАЯ ТРУБА S85 (текущее состояние)

```
✅ Tick = Pure Function Evaluation
✅ Identity Layer L3 + Causal Kernel + L5 Guard
⚠️ NPC стоят на месте — разрыв API между Intent (Role) и Spatial (Node)
🔴 bug_risk: 999.0 ломает фильтр видимости
🔴 SceneStateManager пишет JSON-зеркало в обход API
🟡 GameScreen набит deprecated-фоллбэками
🟡 Спрайты дверей/лестниц/бара — заглушки
```

---

## 4. АРХИТЕКТУРНЫЙ ДОЛГ

| Долг | Приоритет | Описание |
|------|-----------|----------|
| **Spatial Role Routing API** | 🔴 КРИТИЧЕСКИЙ | `SpatialService.resolve_by_role()` для маппинга NodeRole → NodeRef |
| **Activity → Role Mapper** | 🔴 КРИТИЧЕСКИЙ | Конвертация "drinking" → `NodeRole.BAR` |
| **_resolve_position rewrite** | 🔴 КРИТИЧЕСКИЙ | Использование SpatialService вместо хардкода |
| **Bug Risk 999.0** | 🔴 ВЫСОКИЙ | Фикс вычисления дистанции в `player_target_pipeline.py` |
| **JSON Mirror Removal** | 🟡 СРЕДНИЙ | Убрать запись JSON из `scene_state_manager`, перевести GameScreen на API |
| **GameScreen Legacy Fallbacks** | 🟡 СРЕДНИЙ | Вырезать list→dict и deprecated npc_positions |
| **Sprite Placeholder Cleanup** | 🟢 НИЗКИЙ | Замена заглушек на уникальные спрайты |

---

## 5. АКТИВНЫЕ БАГИ

| Баг | Статус | Описание |
|-----|--------|----------|
| NPC стоят на месте | 🔴 ОТКРЫТ | `SpatialService` не отдает узлы по ролям |
| common_area not found | 🔴 ОТКРЫТ | Фоллбэк в `_resolve_position` отправляет в несуществующую зону |
| resolve_node AttributeError | 🔴 ОТКРЫТ | Вызов несуществующего метода в `LifeEngine` |
| Visibility Filter Break | 🔴 ОТКРЫТ | Дистанция 999.0 в `player_target_pipeline` ломает фильтр |
| Дублирование _split_wall_by_openings | ⚠️ ОТКРЫТ | Backend + frontend копии одной функции |

---

## 6. СЛЕДУЮЩИЕ ШАГИ (Строгий порядок)

### 🔴 Фаза 1: Semantic Spatial Binding (КРИТИЧЕСКИЙ)
- [РЕАЛИЗАЦИЯ] Метод `SpatialService.resolve_by_role(role: NodeRole, zone: str = None) -> Optional[NodeRef]`
- [РЕАЛИЗАЦИЯ] Словарь Activity → NodeRole Mapper
- [РЕАЛИЗАЦИЯ] Переписать `LifeEngine._resolve_position` на использование Spatial Routing
- [УДАЛИТЬ] Хардкод `bar_target = "bar_area"` и пустой `_DEFAULT_ACTIVITY_MAP`

### 🔴 Фаза 2: Geography Bug Fixes
- [ФИКС] Устранить `bug_risk: 999.0` в `player_target_pipeline.py`. Если дистанции нет — NPC не должен попадать в список видимых.

### 🟡 Фаза 3: Frontend Debt Cleanup (Слой изоляции)
- [АРХЕОЛОГИЯ] Проверить, какие именно данные фронтенд читает из JSON-зеркала мимо API
- [РЕФАКТОРИНГ] Перевести чтение на `WorldSnapshotDTO`
- [УДАЛИТЬ] JSON mirror код из `scene_state_manager.py` (TODO-A1)
- [УДАЛИТЬ] Legacy fallbacks в `game_screen.py` (list→dict, deprecated fields)

### 🟢 Фаза 4: Visual Differentiation
- [РЕСУРСЫ] Создать/найти уникальные спрайты для `door_transition`, `stairs_up/down`, `bar`, `decoration`
- [УДАЛИТЬ] `TODO: временная заглушка` из `sprite_resolver.py`

---

## 7. КЛЮЧЕВЫЕ ФАЙЛЫ

| Файл | Почему важен |
|------|-------------|
| `backend/app/services/spatial/spatial_service.py` | Добавить `resolve_by_role()` |
| `backend/app/services/npc/life_engine.py` | Переписать `_resolve_position()`, убрать хардкод |
| `backend/app/services/spatial/player_target_pipeline.py` | Фикс `bug_risk: 999.0` |
| `backend/app/services/scene_state_manager.py` | Удалить JSON Mirror (TODO-A1) |
| `frontend/game_screen.py` | Чистка deprecated fallbacks |
| `frontend/sprite_resolver.py` | Замена заглушек |