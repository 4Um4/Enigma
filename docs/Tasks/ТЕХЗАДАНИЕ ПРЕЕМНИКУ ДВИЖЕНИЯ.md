Вот обновлённое Техзадание с зафиксированными результатами нашей сессии. Мы перешли от затыкания дыр к построению физических законов.

***

# ТЕХЗАДАНИЕ ПРЕЕМНИКУ: Спринт 35

**Статус:** Спринт 35 в процессе. БАГ D1, D2 — ЗАКРЫТЫ. Пространственный слой восстановлен. Время стабилизировано. Каузальный мост сознания построен. Критический переход: мир больше не сомнамбула, NPC обладают и телом, и волей.

**Главный принцип:** Мы строим Variable Causal Density. Движение НЕ расширяет онтологию. Макро (LOD1) через `MacroMovementGoal`, микро (LOD0) через `LocalSteeringGoal`. `MovementIntent` = legacy alias для `MacroMovementGoal`.

---

## 0. АРХИТЕКТУРНЫЙ ВЕКТОР (Долгосрочное видение)

Это НЕ задача текущего спринта. Это компас для всех будущих решений.

### Cognitive-Causal Combat & Interaction Model

ENIGMA строит не RPG-бой и не скриптованный нарратив, а **когнитивно-каузальную симуляцию взаимодействий**. Ключевые принципы:

**1. Любое действие проходит 4 стадии (Commitment Pipeline):**
```
Intent Formation → Commitment Window → Execution Point → Consequence Resolution
```
- `Commitment Window` — NPC меняет позу, сокращает дистанцию, достаёт нож. Действие ещё прерываемо.
- `Execution Point` — точка необратимости. После — rollback запрещён.
- Каждое действие имеет: `windup`, `execution_time`, `recovery`. NPC не может spam-attack из-за `recovery`.

**2. Direct MOVE убит полностью (цель).**
Вместо `MOVE command` — `Social Stimulus`:
```
PLAYER_SPOKE(semantic="summon", target="shadow")
→ legitimacy → emotional_impact → pressure_field
→ intent_formation → traversal → speech
```
Речь — ПОСЛЕ физики, не до. Speech is part of embodiment.

**3. Elastic Time во время ввода игрока:**
- Мир не замирает пока игрок пишет
- Tactical dilation: скорость мира снижена, commitment windows растянуты
- Игрок успевает ввести "я закрываюсь стулом" и это реально влияет на `hesitation`, `disgust`, `shock` у NPC

**4. Конкурентные процессы (не линейная очередь):**
Движение, речь, страх, атака, реакция, поза, дистанция, внимание — **конкурируют** одновременно, не выстраиваются в очередь.

**5. Traversal — interruptible:**
```
approach → увидел кровь → остановился → передумал
```
Должно быть возможно через pressure-check на каждом шаге traversal.

**Почему это важно:** Без этого fear/trust/willpower — декоративны. NPC либо телепортируется, либо игнорирует игрока. С этим — мир думает, а не исполняет скрипты.

**ADR который нужно написать перед реализацией:** `ADR-059: Commitment Pipeline & Interruptible Traversal`

---

## ШАГ 0: СИНХРОНИЗАЦИЯ РЕАЛЬНОСТИ (ОБЯЗАТЕЛЬНО)

Перед любым изменением — запросить актуальный код через PowerShell. Опора на кэш = архитектурный баг. В проекте работают параллельные архитекторы.

```powershell
# Проверить состояние активных тестов
python -m pytest backend/tests/sandbox/ -v --tb=short
# Очистить кэш перед запуском
Get-ChildItem -Path "backend/" -Filter "__pycache__" -Recurse -Directory | Remove-Item -Recurse -Force
Get-ChildItem -Path "frontend/" -Filter "__pycache__" -Recurse -Directory | Remove-Item -Recurse -Force
```

---

## 1. ЧТО СДЕЛАНО (Текущая сессия)

1. **Рефакторинг MovementEngine (Sourcery compliance):**
   - Метод `process_intents` разбит на `_resolve_spatial_service`, `_process_single_intent`, `_resolve_micro_movement`, `_resolve_macro_relocation`.
   - Сложность снижена, тесты проходят стабильно (37/37).

2. **БАГ A (Root Cause 1): UTF-8 BOM в `inn_rooms.json`:**
   - Файл имел BOM (`EF BB BF`), `json.loads("utf-8")` выбрасывал `JSONDecodeError`, `try/except` молча проглатывал файл.
   - **Фикс:** В `graph_compiler.py:227` кодировка заменена на `utf-8-sig`, которая корректно обрабатывает BOM.

3. **БАГ A (Root Cause 2): Дубликат ключа `location_id` в `tavern.json`:**
   - Строка 3: `"location_id": "tavern_silver_wolf"`, строка 1248: `"location_id": ""`.
   - Python парсер брал последнее значение (`""`), что включало fallback-условие `if data.get("nodes") and not lid` → таверна подменяла `inn_rooms`.
   - **Фикс:** Удалён дубль `location_id: ""` из `tavern.json`.

4. **БАГ A (Root Cause 3): Опасный fallback в `load_editor_json`:**
   - **Фикс:** Удалено условие `if data.get("nodes") and not lid: return data`. Теперь файл без `location_id` не может стать источником графа для произвольной локации.

5. **БАГ F (Player as NPC): ЗАКРЫТ:**
   - **Фикс:** В `world_snapshot_builder.py` добавлен `if npc_id == "player": continue`.

6. **Кросс-локационное перемещение (ADR-060):**
   - В `SceneChange` добавлено поле `target_location_id: str = ""`. `MovementEngine` передаёт `location_id` из `MovementIntent`. `SceneStateManager` собирает `SpatialService` для целевой локации.

7. **LOD1 Macro-jitter (БАГ E - частичный фикс):**
   - В `scene_state_manager.py` добавлен `random.uniform(-0.4, 0.4)` для макро-узлов.

8. **БАГ G: Первый idle_tick без интентов — ЗАКРЫТ:**
   - **Root Cause 1:** `LifeEngine.get_npc_states()` возвращал `([], [])` (кортеж) вместо `[]` (список) при пустом кэше. Оркестратор получал кортеж из двух пустых списков вместо NPC-диктов.
   - **Root Cause 2:** `idle_tick()` вызывал `get_scene_state()` напрямую вместо `ensure_scene_initialized()`, пропуская инициализацию сцены.
   - **Фикс G-1:** `return []` вместо `return [], []`.
   - **Фикс G-2:** `idle_tick()` теперь вызывает `ensure_scene_initialized(self, campaign_id)`.

9. **БАГ H: Время сбрасывается при диалоге — ЗАКРЫТ:**
   - **Root Cause:** При `scene_state is None` (реинициализация сцены), время сбрасывалось к дефолту "07:00". Абсолютное `game_time_seconds` из `shared_context` не сохранялось.
   - **Фикс:** Перед `get_scene_state()` сохраняется `_preserved_game_time` из `shared_context`. При создании новой сцены время наследуется.

10. **БАГ I: NPC исчезают/0,0 при смене локации — ЗАКРЫТ:**
    - **Root Cause 1:** `_extract_npc_positions` не фильтровал NPC по локации.
    - **Root Cause 2:** `apply_changes` ставил `entry["location"]` но не `entry["location_id"]` — рассинхрон ключей.
    - **Фикс I-1:** Добавлена фильтрация `npc_loc != current_location → continue`.
    - **Фикс I-2:** Унификация `entry["location_id"] = target_loc` рядом с `entry["location"]`.

11. **ДОЛГ 2: DecisionHub даёт 0 решений — ЗАКРЫТ:**
    - **Root Cause:** `tick_decisions()` передавал `EventType.IDLE` вместо `EventType.WORLD_TICK`. Это убивало все PROACTIVE_INTENTS и подавляло давление (decay вместо аккумуляции). За 18 тиков — 0 решений.
    - **Фикс:** `EventContext(event_type=EventType.WORLD_TICK, ...)`. Теперь NPC генерируют проактивные интенты в idle tick.

12. **Sourcery: Рефакторинг `scene_init.py`:**
   - Сложность снижена с 48 до ~6 на функцию.
   - `init_scene_state` разрезан на 11 подфункций: `_resolve_initial_time`, `_extract_preserved_time`, `_materialize_npc_inventory`, `_update_player_position`, `_sync_game_time`, `_inject_spatial_service`, `_reconcile_elapsed_time`, `_apply_life_changes`, `_run_life_engine_tick`, `_check_economy_tracker`, `_load_or_create_scene`.

13. **Тесты: Обновление под новый контракт движения:**
   - `test_micro_macro_locomotion.py`: убрана проверка `target_node_id` (нет у `LocalSteeringGoal`).
   - `test_lod0_collision_avoidance.py`: `MovementIntent` заменён на `LocalSteeringGoal`.
   - Все 37 sandbox-тестов зелёные.

14. **БАГ D (Lerp): Tick-based Dual Time Ontology (Сессия 41):**
   - **Root Cause 1:** Вычисление каузального тика через календарное время (`game_time_seconds // 60`) создавало круговую зависимость. При откате календаря тик становился отрицательным, `progress` обнулялся, NPC застывали.
   - **Root Cause 2:** `WorldSnapshotBuilder` мутировал `scene_state` (удалял завершённые транзиты, перезаписывал `local_position`), нарушая принцип чистой проекции.
   - **Root Cause 3:** `_enrich_local_positions` перезаписывал `local_position` при наличии активного транзита и имел фоллбэк на `entrance` (вызывал телепортацию к двери).
   - **Фикс:** Внедрен монотонный каузальный тик `scene_state["tick"]` (инкремент +1 каждый тик). `TraversalState` переведён на `started_tick` и `duration_ticks`. `WorldSnapshotBuilder` стал чистой проекцией. Убиты фоллбэки на `entrance` и `real_time`. Lerp работает, NPC по расписанию ходят плавно.

15. **БАГ D1: LegitimacyGate блокирует подчинение — ЗАКРЫТ:**
   - **Root Cause:** Формула `legitimacy = max(fear, trust)` и порог `> 0.3` делали бесстрашных NPC полностью глухими к приказам. Смешение внутреннего состояния и внешней силы в один скаляр.
   - **Фикс:** Внедрена Физика Власти. `AuthorityPressure` (внешняя сила: `source_authority * context_modifier`) отделяется от `Legitimacy` (внутренняя готовность: `max(fear, trust)`). Подчинение: `authority_pressure * (0.3 + legitimacy)`. Раздражение: `authority_pressure * 0.3 * (1.0 - legitimacy)`. Бесстрашный вор теперь реагирует на вооружённый приказ раздражённой конфронтацией, а не игнором. `is_obedience` определяется легитимностью, а не амплитудой давления.

16. **БАГ D2: Сущность Player рендерится как NPC — ЗАКРЫТ:**
   - **Root Cause:** Хардкод `if npc_id == "player": continue` на бэкенде и отсутствие фильтра на фронтенде.
   - **Фикс:** Внедрена онтология `EntityType` (PLAYER, NPC, ANIMAL, OBJECT). `NPCPositionDTO` содержит поле `entity_type`. Бэкенд помечает сущности, фронтенд фильтрует строго по типу (`if entity_type != "npc": continue`), защищая презентационный слой от любых утечек.

17. **Spatial Fallback (Граф не найден) — ЗАКРЫТ:**
   - **Root Cause:** Опасный fallback был удалён ранее, но легитимный путь до `location_templates.json` не был проложен. Папки кампаний не содержали отдельных JSON локаций.
   - **Фикс:** В `graph_compiler.py` добавлен безопасный fallback на `location_templates.json` с поиском точного совпадения `location_id`. Шаблон инстанцируется через `copy.deepcopy()`, чтобы мутации в `scene_state` не ломали "идею" локации.

18. **БАГ H (Регресс): Время откатывается в полночь — ЗАКРЫТ:**
   - **Root Cause:** Проверка `if preserved_game_time:` отбрасывала валидное значение `0.0` (полночь).
   - **Фикс:** Заменено на `if preserved_game_time is not None:` в `_extract_preserved_time`, `_resolve_initial_time` и API-границе.

19. **SHI=0% (Мир сомнамбула): Каузальный мост сознания — ЗАКРЫТ:**
   - **Root Cause:** В idle-пути `tick_decisions` генерировал когнитивные решения (FLEE, APPROACH), но они **никогда не конвертировались в MovementIntent**. Тело двигалось по расписанию, но было глухо к сознанию. Давление накапливалось слишком медленно для движения.
   - **Фикс:** `tick_decisions` теперь напрямую возвращает `idle_movement_goals`. `TickOrchestrator` в Фазе 5 конвертирует их в `MacroMovementGoal` и передаёт в `MovementEngine` мгновенно, не дожидаясь накопления давления (давление нужно только для речи/телеграфа).

---

## 2. АКТИВНЫЕ БАГИ (Блокеры)

*(Критических блокеров нет. Все основные баги закрыты.)*

### thief_shadow=None (Координаты теряются)
**Статус:** НИЗКИЙ.

**Симптом:** `thief_shadow` иногда имеет координаты `None` в CDS, несмотря на наличие Traversal.
**Подозрение:** Вторичный симптом. Возможен race condition при инициализации позиции перед первым транзитом, или некорректный `spawn_node` при старте.

### Отсутствующие карты: `city_gate.json`, `market_square.json`
**Статус:** СРЕДНИЙ. 
**Примечание:** Fallback на `location_templates.json` позволяет NPC существовать, но для полноценного кросс-локационного перемещения (ADR-060) нужны отдельные графы через Map Editor.

---

## 3. АРХИТЕКТУРНЫЙ ДОЛГ

### ДОЛГ 1: Cognitive Causal Timing — Stale Cognition
**Приоритет:** ВЫСОКИЙ.

**Проблема:**
```
Tick N:   DirectiveInterpretationSubscriber создал pressure → delta_buffer
          DecisionHub НЕ видит этот delta (он в будущем commit)
Tick N+1: pressure применилась → но ситуация уже изменилась
```
NPC думают о реальности прошлого тика. Это не баг реализации — это онтологическая ошибка pipeline.

### ДОЛГ 3: `location_templates.json` vs Editor JSON — двойной источник истины
**Приоритет:** СРЕДНИЙ.

Инстанциация через `deepcopy` решает проблему мутации шаблонов, но физически отсутствуют карты `city_gate.json` и `market_square.json`, что ограничивает перемещение `guard_borko` и `merchant_goran` в пределах таверны (через fallback-узлы).

---

## 4. СЛЕДУЮЩИЕ ШАГИ (Строгий порядок)

1. **[ИССЛЕДОВАНИЕ] thief_shadow=None:**
   - Проверить логику спавна в `_enrich_local_positions` и `MovementEngine`. Убедиться, что NPC без `local_position` получают координаты `spawn_node` до начала транзита.

2. **[БЛОКЕР] Создать `city_gate.json` и `market_square.json`:**
   - Через Map Editor для поддержки ADR-060.

3. **[ИССЛЕДОВАНИЕ] ДОЛГ 1: Stale Cognition — ADR-059.**

---

## 5. Актуальные файлы

- `backend/app/services/social/directive_interpretation_subscriber.py` — Физика Власти (AuthorityPressure * Receptivity)
- `backend/app/domain/snapshot.py` — EntityType Enum, NPCPositionDTO.entity_type
- `backend/app/services/integration/world_snapshot_builder.py` — Проброс entity_type, чистая проекция
- `frontend/game_types.py` — Расширен Literal для entity_type
- `frontend/game_screen.py` — Онтологический фильтр рендера (entity_type != "npc")
- `backend/app/services/spatial/graph_compiler.py` — Легитимный fallback на location_templates.json + deepcopy
- `backend/app/services/game_loop/scene_init.py` — Защита полуночи (is not None), preserved time
- `backend/app/services/game_loop/__init__.py` — Защита API-границы (getattr вместо or)
- `backend/app/services/npc/life_engine.py` — Прямые idle_movement_goals, фикс цели намерения
- `backend/app/services/tick_orchestrator.py` — Каузальный мост сознания (Фаза 5: DecisionHub → MovementEngine)

---

## Статус багов

| Баг | Статус | Примечание |
|-----|--------|------------|
| БАГ A: NPC на нулях | ✅ ЗАКРЫТ | BOM + дубль + fallback починены |
| БАГ B: Телепортация (нет lerp) | ✅ ЗАКРЫТ | TraversalState создаётся, lerp работает |
| БАГ C: WASD → DM | ✅ ЗАКРЫТ | Спринт 34 |
| БАГ D: NPC телепортируются | ✅ ЗАКРЫТ | Tick-based Dual Time (ADR-059). Lerp работает |
| БАГ D1: LegitimacyGate блокирует подчинение | ✅ ЗАКРЫТ | Физика Власти. Бесстрашные NPC конфронтируют |
| БАГ D2: Player рендерится как NPC | ✅ ЗАКРЫТ | Онтологический тип (EntityType). Фильтр на фронтенде |
| БАГ E: NPC сливаются в узле | ✅ ЗАКРЫТ | Macro-jitter добавлен |
| БАГ F: Player как NPC | ✅ ЗАКРЫТ | Фильтрация в world_snapshot_builder |
| БАГ G: Первый тик без интентов | ✅ ЗАКРЫТ | get_npc_states + ensure_scene_initialized |
| БАГ H: Время сбрасывается | ✅ ЗАКРЫТ | Preserved game_time_seconds, защита полуночи |
| БАГ I: NPC исчезают/0,0 | ✅ ЗАКРЫТ | Фильтрация по локации + унификация location_id |
| ДОЛГ 2: 0 решений | ✅ ЗАКРЫТ | EventType.WORLD_TICK вместо IDLE |
| SHI=0%: Мир сомнамбула | ✅ ЗАКРЫТ | Каузальный мост: сознание напрямую управляет телом |