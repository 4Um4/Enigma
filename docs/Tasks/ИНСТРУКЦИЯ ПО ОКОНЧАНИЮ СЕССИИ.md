"""
ИНСТРУКЦИЯ ПО ОКОНЧАНИЮ СЕССИИ (Спринта)
После каждого крупного цикла (спринта) ОБЯЗАТЕЛЬНО обновить 4 документа.
Без них ИИ в новой сессии будет слеп — MUTATIONS.md даёт историю, но не понимание.

Чеклист (5 пунктов)
1. MUTATIONS.md — ЧТО произошло
Формат: [SESS_XX] Дата: Описание изменения (что добавлено/удалено).
Пример: [SESS_13] 27.05.26: Создана _enrich_with_social_relations() в npc_loader.py

2. ADR — ПОЧЕМУ принято решение
Формат ADR:

Title: Краткое название решения
Context: Какая проблема решалась
Decision: Что решили и почему именно так
Consequences: Положительные и отрицательные последствия
Файл: docs/Tasks/ADR (Architecture Decision Records).md
Нумерация: ADR-001, ADR-002, ... (сквозная)

3. DTO Registry — АКТУАЛЬНЫЕ контракты
Все DTO и их поля. Если ИИ забудет какие поля у StateDeltas — он начнёт их придумывать.
Обновлять: новые DTO, изменённые поля, удалённые поля.

Файл: docs/Tasks/DTO Registry (Реестр контрактов).md

4. ARCHITECTURE_FLOW — топологическая карта
Mermaid flowchart TD — единственный способ увидеть систему целиком.
Добавлять новые узлы и потоки данных после каждого спринта.

Файл: docs/Tasks/ARCHITECTURE_FLOW.md

5. ТЕХЗАДАНИЕ ПРЕЕМНИКУ — передать контекст следующей сессии
Обновить: текущее состояние системы, приоритеты, баги, ключевые файлы.
Это «снимок памяти проекта» — с ним новая LLM-сессия подхватит контекст за 30 секунд.

Почему это важно
MUTATIONS.md говорит: «Создан CombatResolver как pure function».
ADR говорит ПОЧЕМУ: «LLM галлюцинирует при насилии, Python считает урон, мутация только через StateApplicator».

Если ADR нет — через месяц ИИ скажет: «Пусть CombatResolver сам меняет стейт, так быстрее». И сломает всё.

Если DTO Registry устарел — ИИ начнёт придумывать поля, которых нет.
Если flowchart не обновлён — ИИ не увидит, что CombatResolver должен отдать дельту в StateApplicator.

Набор файлов проекта (снимок памяти)
АРХИТЕКТУРНЫЙ_УСТАВ_ENIGMA.md — законы (нарушение = баг)
РЕЖИМ РАБОТЫ.md — режим работы (пошаговый, один шаг = одно изменение)
ADR (Architecture Decision Records).md — ПОЧЕМУ приняты решения
DTO Registry (Реестр контрактов).md — АКТУАЛЬНЫЕ контракты всех DTO
ARCHITECTURE_FLOW.md — топологическая карта (Mermaid)
MUTATIONS.md — ЧТО произошло (история изменений)
ТЕХЗАДАНИЕ ПРЕЕМНИКУ — текущее состояние + приоритеты
ИНСТРУКЦИЯ ПО ОКОНЧАНИЮ СЕССИИ.md — этот файл (чеклист)
"""