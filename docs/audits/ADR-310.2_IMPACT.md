# ADR-310.2 Impact Audit
> Этот файл — детальный аудит ОДНОГО ADR. Единый атлас всех ADR: `docs/ADR (Architecture Decision Records).md`

`ADR-310.2` [STANDARD] **IMPACT**

﻿# ADR-310.2 Impact Audit
> Этот файл — детальный аудит ОДНОГО ADR. Единый атлас всех ADR: `docs/ADR (Architecture Decision Records).md`

`ADR-310.2` [STANDARD] **IMPACT**
# ADR-310.2_IMPACT
## Changed Domains
- DOM-01 (Foundation): Внедрена проверка валидности сущностей перед release.
- DOM-02 (Will & Decision): Протухшие интенты прерываются, а не исполняются.

## Downstream Consumers
- **TickOrchestrator**: Фаза 7 проверяет `life_status` актёра и цели, а также их присутствие в сцене.
- **EventBus**: Защищён от публикации событий с мёртвыми или отсутствующими участниками.

## Runtime Impact
- RAM: 0
- Latency: 0
- Execution correctness повышена.

## Sandbox Tests
- `DriftLaboratory` (200 тиков): `comparisons=348 rate=1.740/tick`, 0 крашей.

## Rollback
- Revert changes in `backend/app/services/tick_orchestrator.py` (убрать блок валидации в Фазе 7).
