memory_manager.py: Когда менять число
  Уменьшить до 10-12, если:
  Сцены короткие (таверна: заказал-получил-выпил-вышел)
  Токены критичны (7B модель с 2K контекстом)
  Увеличить до 30-40, если:
  Длинные дипломатические переговоры (одна сцена = 20 ходов)
  Низкий decay (память медленно тухнет)
  Используете summary compression (сжимаете старые 10 событий в 1 summary)
  4. Рекомендация прямо сейчас
  Оставьте 20, но добавьте метрику:
  # В MemoryManager
  def get_memory_context(self, npc_id: str) -> str:
      events = self.working_memory.get_recent(self.WORKING_MEMORY_SIZE)
      estimated_tokens = sum(len(e.description) for e in events) / 4  # ~4 chars/token
      
      if estimated_tokens > 800:
          # Автоматически сжимать самые старые при переполнении
          return self._compress_old_events(events)
      
      return self._format_events(events)
      
  Если в тестах (R4.1) NPC будут "терять нить" разговора — поднимайте до 25-30. Если контекст переполняется — сбрасывайте до 15.


***

Починить атомарность run_decay_if_needed:
    Дополнительное улучшение
    Добавьте rollback на уровне WorkingMemory, если replace_all не поддерживает транзакционность.
***

 def get_weights_for_decision:
    Рекомендации по коду:
    1. Добавить safety-check на decay:
    Python
    Copy
    def get_weights_for_decision(self, ...):
        # Гарантируем свежесть данных перед чтением
        self.run_decay_if_needed(campaign_id, current_tick)
        
        rel = self._relationships.get_pair(...)
        recent = self._working.get(campaign_id)
        # ...
    2. Уточнить логику recent_pressure:
    Сейчас вы суммируете importance, но в спецификации (п.7) эмоция = emotion_base + Σ(event_deltas × decay). Если событие "harassment" даёт delta -10 к trust, возможно стоит суммировать именно эти дельты, а не абстрактную важность?
    Вариант А (текущий): pressure = сумма важности (количественная загруженность истории)
    Вариант Б (ближе к формуле): pressure = сумма emotional_deltas (качественный заряд)
    Для R1 (текущая фаза) — ваш вариант правильный. Для R4 (калибровка) — проверьте, что 100 не слишком много при вашем DECAY_EVERY.
    3. Исключить архивированные события:
    Ваш код фильтрует archived в run_decay_if_needed, но get_weights_for_decision читает "сырую" working memory. Убедитесь, что _working.get() возвращает уже отфильтрованный список (без archived), или добавьте фильтр здесь тоже.


***
working_memory.py:
    def replace_all:
    Мелкое улучшение
    Можно заменить цикл на extend для читаемости (функционально то же самое):
    Python
    Copy
    def replace_all(self, campaign_id: str, events: List[Dict[str, Any]]) -> None:
        new_buf: deque = deque(maxlen=self._maxlen)
        new_buf.extend(events)  # Быстрее и чище, чем for+append
        self._buffers[campaign_id] = new_buf
    Вывод: Добавление replace_all — критически важный шаг для R1 (Memory Core). Он превращает рабочую память из "опасной мутабельной структуры" в "атомарный снимок состояния", что соответствует архитектуре ENIGMA

***

importance_engine.py:
    def score_event:
    Дополнительная защита
    Для полной безопасности добавьте валидацию ключей при старте системы:
    Python
    Copy
    # В начале importance_engine.py
    def _validate_rules():
        """Проверяем, что ключи не являются подстроками друг друга."""
        keys = list(IMPORTANCE_RULES.keys())
        for i, k1 in enumerate(keys):
            for k2 in keys[i+1:]:
                if k1 in k2 or k2 in k1:
                    import warnings
                    warnings.warn(f"Ambiguous importance keys: '{k1}' and '{k2}'")

    _validate_rules()

***

